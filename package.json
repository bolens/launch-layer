{
  "name": "launchlayer",
  "private": true,
  "description": "Scripts-only root shim — install and run hub tooling from hub/ (or via scripts/hub-pm.sh).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "bash scripts/hub-pm.sh run dev",
    "test:hub": "bash scripts/hub-pm.sh run test",
    "lint:hub": "bash scripts/hub-pm.sh run lint",
    "convex:deploy": "bash scripts/hub-pm.sh run convex:deploy"
  },
  "license": "MIT"
}
